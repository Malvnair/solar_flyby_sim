name: solar-flyby
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - numpy>=1.26
  - pandas>=2.2
  - matplotlib>=3.9
  - pyyaml>=6.0
  - numba>=0.59
  - pyarrow>=16.0
  - rebound              # conda-forge has this
  - pip
  - pip:
      - pytest           # so tests are ready
